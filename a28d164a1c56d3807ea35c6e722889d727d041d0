## Что сделано

### Статика
- \pp.css\ — полный набор стилей: карточки, пагинация, поиск, flash-сообщения, формы, кастомный скроллбар
- \pp.js\ — заглушка; pin toggle полностью через Alpine.js в шаблоне

### Ключевые решения
- Единый CSS-файл без препроцессоров — минимальная зависимость, не нужен сборщик
- Pin-обработчик в шаблоне, а не в JS — контекст (ID заметки, CSRF-токен) доступен напрямую из Go
